# Architecture

## Components
1. Report Runner (`src/reconcile/runner.ts`)
2. Report Writer (`src/reconcile/writer.ts`)
3. Log Adapter (`src/reconcile/logging.ts`)

## Ownership Hints
1. Story ST-201 owns `src/reconcile/runner.ts`.
2. Story ST-202 owns `src/reconcile/writer.ts`.
